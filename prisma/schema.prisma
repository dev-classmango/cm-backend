generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PG_DATABASE_URL")
}

model User {
  id    String  @id @unique
  email String  @unique
  name  String?

  @@map("users")
}
